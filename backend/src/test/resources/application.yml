spring:
  profiles:
    active: integration
  flyway:
    enabled: false

sse:
  heartbeat:
    enabled: true
    interval-ms: 25000
---
spring:
  config:
    activate:
      on-profile: e2e

---
spring:
  config:
    activate:
      on-profile: integration
